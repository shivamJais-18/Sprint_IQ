# SprintIQ Frontend

React + Vite frontend for SprintIQ.

## Local VS Code setup

```bash
npm install
npm run dev
```

Create `.env` from `.env.example`:

```env
VITE_API_URL=http://localhost:8080/api
```

The backend must be running separately on port 8080.
